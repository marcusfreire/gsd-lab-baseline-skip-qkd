---
phase: 01-phase-0-contracts-and-scaffolding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/archive/baseline.md
  - docs/architecture.md
  - docs/phase-0-plan.md
  - baseline.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PROTO-01
  - PROTO-04
user_setup: []
must_haves:
  truths:
    - "README.md explains baseline purpose, local topology, Phase 0 scope, and deferred implementation."
    - "docs/architecture.md identifies the KME mock, KeyProvider-A, KeyProvider-B, simulated encryptors, persistence boundaries, and future IKEv2/RFC8784 boundary."
    - "docs/phase-0-plan.md decomposes Phase 0 tasks, dependencies, risks, mitigations, and acceptance criteria before implementation code exists."
    - "baseline.md is archived under docs/archive/baseline.md and no longer acts as the normative root document."
    - "Decision coverage: D-01, D-02, D-03, and D-13 are implemented by the README, archive move, architecture doc, and Phase 0 plan."
  artifacts:
    - path: README.md
      why: "Human entry point for the baseline."
    - path: docs/archive/baseline.md
      why: "Historical source retained without making it normative."
    - path: docs/architecture.md
      why: "Architecture contract for Phase 0."
    - path: docs/phase-0-plan.md
      why: "Detailed technical plan, risks, and acceptance criteria."
  key_links:
    - "README.md links to docs/architecture.md, docs/phase-0-plan.md, docs/api-etsi014-mock.md, docs/api-skip.md, docs/data-model.md, docs/test-plan.md, and docs/security-assumptions.md."
    - "docs/phase-0-plan.md references draft-singh-skip-00, the minimal ETSI 014 mock profile, provider independence, and simulated encryptors as first consumers."
---

<objective>
Create the human-facing Phase 0 baseline overview, architecture contract, technical plan, and historical archive boundary.

Purpose: Make the repository understandable before service logic exists and prevent the original baseline notes from competing with normative docs.
Output: `README.md`, `docs/archive/baseline.md`, `docs/architecture.md`, and `docs/phase-0-plan.md`.
</objective>

<execution_context>
@/home/marcusfreire/src/rascunho/gsd/.codex/get-shit-done/workflows/execute-plan.md
@/home/marcusfreire/src/rascunho/gsd/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-phase-0-contracts-and-scaffolding/01-CONTEXT.md
@.planning/phases/01-phase-0-contracts-and-scaffolding/01-RESEARCH.md
@.planning/phases/01-phase-0-contracts-and-scaffolding/01-PATTERNS.md
@baseline.md
</context>

## Artifacts this phase produces

- `README.md` - repository entry point and Phase 0 status.
- `docs/archive/baseline.md` - archived original baseline content.
- `docs/architecture.md` - normative architecture contract.
- `docs/phase-0-plan.md` - Phase 0 technical plan, risks, dependencies, and acceptance criteria.
- Concepts documented: KME mock, KeyProvider-A, KeyProvider-B, simulated encryptor, per-provider SQLite boundary, future RFC8784/Cisco boundary.

Decision coverage:
- D-01: `docs/*.md` become normative project contracts after Phase 0.
- D-02: root `baseline.md` is historical input, not the normative contract.
- D-03: `baseline.md` is moved to `docs/archive/baseline.md`.
- D-13: Phase 0 acceptance depends on required files, internal consistency, scaffold directories, and parseable Compose configuration.

<threat_model>
Scope: Documentation and repository structure only; no service logic or real key handling is introduced by this plan.

Assets:
- Protocol authority in `docs/*.md`.
- Historical source in `docs/archive/baseline.md`.
- Future implementation boundaries for KME mock, Key Providers, and encryptor simulators.

Threats and mitigations:
- MEDIUM: Architecture docs imply production security or full ETSI 014 conformance. Mitigate by explicitly labeling the ETSI API as a minimal logical mock and security posture as local/non-production.
- MEDIUM: README hides future Cisco/IKEv2 boundaries. Mitigate by naming Cisco and RFC8784 as future work, with simulated encryptors first.
- MEDIUM: Provider independence is described vaguely. Mitigate by requiring separate KeyProvider-A/B instances and separate planned SQLite state in architecture diagrams and text.
- LOW: Archived baseline is mistaken for current contract. Mitigate by adding an archive notice at the top of `docs/archive/baseline.md` and removing root `baseline.md`.

Blockers: No HIGH severity threats are introduced because this plan only creates documentation and archival structure.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Archive the original baseline and establish README authority</name>
  <files>baseline.md, docs/archive/baseline.md, README.md</files>
  <read_first>baseline.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-phase-0-contracts-and-scaffolding/01-CONTEXT.md</read_first>
  <action>Create `docs/archive/` if missing. Move the current `baseline.md` content to `docs/archive/baseline.md`; add an archive notice stating that the file is historical input and that `docs/*.md` files are normative after Phase 0. Remove the root `baseline.md` file after the archived copy exists. Create `README.md` with sections named exactly `## Purpose`, `## Phase 0 Scope`, `## Local Topology`, `## Key Contracts`, `## What Is Not Implemented Yet`, and `## Repository Layout`. The README must state that the project integrates a logical ETSI GS QKD 014 mock, independent Key Providers, and simulated encryptors; it must also state that real Cisco encryptors and real IKEv2/RFC8784 are deferred.</action>
  <verify>test -f README.md && test -f docs/archive/baseline.md && test ! -f baseline.md && rg -n "## Purpose|## Phase 0 Scope|## Local Topology|draft-singh-skip-00|simulated encryptors|RFC8784" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `## Purpose`.
    - `README.md` contains `## Phase 0 Scope`.
    - `README.md` contains `## Local Topology`.
    - `README.md` contains `draft-singh-skip-00`.
    - `README.md` contains `simulated encryptors`.
    - `README.md` contains `RFC8784`.
    - `docs/archive/baseline.md` contains `Historical source`.
    - Root `baseline.md` no longer exists.
  </acceptance_criteria>
  <done>`README.md` is the human entry point and the original baseline is archived with explicit non-normative status.</done>
</task>

<task type="auto">
  <name>Task 2: Write the architecture contract</name>
  <files>docs/architecture.md</files>
  <read_first>docs/architecture.md, README.md, .planning/PROJECT.md, .planning/research/ARCHITECTURE.md, .planning/phases/01-phase-0-contracts-and-scaffolding/01-RESEARCH.md, .planning/phases/01-phase-0-contracts-and-scaffolding/01-PATTERNS.md</read_first>
  <action>Create `docs/architecture.md` with sections named exactly `## Scope`, `## Components`, `## Data Flow`, `## Provider Independence`, `## Persistence Boundary`, `## Identifier Mapping`, `## Runtime Topology`, and `## Future Boundaries`. Document these components by name: `kme-mock`, `keyprovider-a`, `keyprovider-b`, `encryptor-a-sim`, and `encryptor-b-sim`. State that each Key Provider is an ETSI 014 mock client toward the KME and a SKIP server toward its encryptor. State that KeyProvider-A and KeyProvider-B use the same configurable source directory but distinct runtime configuration and SQLite state. State that RFC8784 PPK and Cisco integration are future boundaries, not Phase 0 implementation.</action>
  <verify>test -f docs/architecture.md && rg -n "## Components|## Provider Independence|keyprovider-a|keyprovider-b|ETSI 014 mock client|SKIP server|SQLite|RFC8784|Cisco" docs/architecture.md</verify>
  <acceptance_criteria>
    - `docs/architecture.md` contains `## Components`.
    - `docs/architecture.md` contains `## Provider Independence`.
    - `docs/architecture.md` contains `keyprovider-a`.
    - `docs/architecture.md` contains `keyprovider-b`.
    - `docs/architecture.md` contains `ETSI 014 mock client`.
    - `docs/architecture.md` contains `SKIP server`.
    - `docs/architecture.md` contains `SQLite`.
    - `docs/architecture.md` contains `RFC8784`.
    - `docs/architecture.md` contains `Cisco`.
  </acceptance_criteria>
  <done>`docs/architecture.md` makes topology, roles, state boundaries, and future integration boundaries explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Write the Phase 0 technical plan</name>
  <files>docs/phase-0-plan.md</files>
  <read_first>docs/phase-0-plan.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-phase-0-contracts-and-scaffolding/01-CONTEXT.md, .planning/phases/01-phase-0-contracts-and-scaffolding/01-RESEARCH.md</read_first>
  <action>Create `docs/phase-0-plan.md` with sections named exactly `## Objective`, `## Task Decomposition`, `## Dependencies`, `## Risks and Mitigations`, `## Phase 0 Acceptance Criteria`, and `## Deferred Work`. Include four task groups with these names: `Documentation baseline`, `Protocol and data contracts`, `Security and verification`, and `Infrastructure scaffolding`. Include explicit risks and mitigations for `Incomplete SKIP conformance against draft-singh-skip-00`, `Accidental centralization of Key Provider state`, and `Confusing the ETSI 014 mock with full ETSI GS QKD 014 conformance`. Acceptance criteria must include required files, required service directories, internal documentation consistency, and optional `docker compose -f infra/docker-compose.yml config` when Docker Compose is available.</action>
  <verify>test -f docs/phase-0-plan.md && rg -n "## Task Decomposition|Incomplete SKIP conformance against draft-singh-skip-00|Accidental centralization of Key Provider state|full ETSI GS QKD 014 conformance|docker compose -f infra/docker-compose.yml config" docs/phase-0-plan.md</verify>
  <acceptance_criteria>
    - `docs/phase-0-plan.md` contains `## Task Decomposition`.
    - `docs/phase-0-plan.md` contains `Incomplete SKIP conformance against draft-singh-skip-00`.
    - `docs/phase-0-plan.md` contains `Accidental centralization of Key Provider state`.
    - `docs/phase-0-plan.md` contains `full ETSI GS QKD 014 conformance`.
    - `docs/phase-0-plan.md` contains `docker compose -f infra/docker-compose.yml config`.
    - `docs/phase-0-plan.md` contains `Real Cisco encryptor integration`.
    - `docs/phase-0-plan.md` contains `Real IKEv2/RFC8784 integration`.
  </acceptance_criteria>
  <done>`docs/phase-0-plan.md` satisfies the user's request for a detailed technical plan before implementation code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f README.md && test -f docs/archive/baseline.md && test -f docs/architecture.md && test -f docs/phase-0-plan.md`
- [ ] `test ! -f baseline.md`
- [ ] `rg -n "draft-singh-skip-00|ETSI GS QKD 014|KeyProvider-A|KeyProvider-B|simulated encryptors|RFC8784" README.md docs/architecture.md docs/phase-0-plan.md`
- [ ] `rg -n "Incomplete SKIP conformance against draft-singh-skip-00|Accidental centralization of Key Provider state|full ETSI GS QKD 014 conformance" docs/phase-0-plan.md`
</verification>

<success_criteria>

- All tasks completed.
- Required files exist and `baseline.md` is archived.
- Architecture describes KME mock, KeyProvider-A, KeyProvider-B, simulated encryptors, per-provider persistence, and future RFC8784/Cisco boundaries.
- Phase 0 technical plan includes task decomposition, dependencies, risks, mitigations, and acceptance criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/01-phase-0-contracts-and-scaffolding/01-01-SUMMARY.md`
</output>
